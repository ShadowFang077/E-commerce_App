import 'package:flutter/material.dart';

class all_features extends StatelessWidget {
  const all_features({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('All Featured',style: TextStyle(fontWeight: FontWeight.bold),);
  }
}
